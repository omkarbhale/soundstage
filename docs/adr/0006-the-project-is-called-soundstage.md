# The project is called soundstage

A soundstage is the space a studio keeps ready so nothing has to be built from scratch for
each production, which is exactly what this repo is. The working folder name `video-maker`
described the category rather than the thing, and it was chosen before it was clear that
the repo is not the tool that makes the video.
