# The project is called soundstage

A soundstage is the space a studio keeps ready so nothing has to be built from scratch for
each production, which is exactly what this repo is.

## Considered Options

`video-maker` was rejected: it names the category rather than the thing, and this repo is
not the tool that makes the video.
